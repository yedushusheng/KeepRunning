


<view clas="mainView">
    <!--标题-->

    <view class="header">
        <image class="icon" src="/resources/keeprunning.png" mode="aspectFill"/>
        <view class="bigTitle">KeepRunning</view>
        <view class="desc">On the way--yedushusheng</view>
    </view>
    
    
    
    <view class="body">
        <view class="widget">
            <block wx:for="{{pageNames}}">
                <view class="widgets__item">
                    <navigator url="{{item.id}}/{{item.id}}">{{item.name}}</navigator>
                </view>
            </block>
        </view>
    </view>

</view>
    