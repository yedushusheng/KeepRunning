

<view class="head" style="flex-direction:row;">
    <image class="icon" src="/resources/joyrun.png" mode="aspectFill"/>
    <button bindtap="openLocation">定位</button>
    <button bindtap="starRun">跑步啦</button>
    <button bindtap="stopRun">等等，累死仙女了</button>
    <text>\n哎呀，跑了这么远了：{{meters}}km</text>
    <text>\n\n我看看跑多久了：{{time}}</text>
</view>

<view class="mainView">
   <map
        class="mapView"
        style="width: 100%; height: 375px;"
        latitude="{{latitude}}"
        longitude="{{longitude}}"
        markers="{{markers}}"
        covers="{{covers}}"
        >
    </map>

</view>

